export const systemRole={
    USER: "user",
    ADMIN: "admin"
}
Object.freeze(systemRole)

export const taskType={
    SHARED: "shared",
    PRIVATE: "private"
}
Object.freeze(taskType)

